name: Deploy to EasyWP

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Deploy via SFTP (EasyWP)
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          port: 22

          # Local folder to upload (your repo)
          local_path: "./"

          # Folder inside your SFTP root
          remote_path: "lab"

          # Overwrite existing files but do not delete
          delete_remote_files: false

          # Ignore git and workflow meta
          exclude: ".git*,.github*"

          # Force pure SFTP mode only
          sftp_only: true
